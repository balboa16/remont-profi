# Ремонт-Профи

Профессиональный лендинг для компании по ремонту квартир и домов.

## Структура проекта

```
remont-profi/
├── index.html      # Главная страница
├── style.css       # Стили
├── main.js         # Интерактивность
└── README.md       # Документация
```

## Возможности

- Адаптивный дизайн (мобильные устройства, планшеты, десктоп)
- Плавная прокрутка к секциям
- Мобильное меню
- Маска ввода телефона (+7 формат)
- Валидация формы
- Анимации при скролле
- Кнопка "Наверх"

## Локальный запуск

### Вариант 1: Просто открыть в браузере

Дважды кликните на файл `index.html` — сайт откроется в браузере.

### Вариант 2: Локальный сервер (рекомендуется)

**С помощью Python:**
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```
Откройте http://localhost:8000

**С помощью Node.js:**
```bash
# Установите serve глобально
npm install -g serve

# Запустите
serve .
```

**С помощью VS Code:**
Установите расширение "Live Server" и нажмите "Go Live" в правом нижнем углу.

## Деплой на Vercel

### Способ 1: Через GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/remont-profi.git
   git push -u origin main
   ```

2. **Подключите к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "Add New Project"
   - Импортируйте репозиторий из GitHub
   - Нажмите "Deploy"

3. **Готово!** Ваш сайт будет доступен по адресу вида `remont-profi.vercel.app`

### Способ 2: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Авторизуйтесь:**
   ```bash
   vercel login
   ```

3. **Задеплойте:**
   ```bash
   vercel
   ```
   Следуйте инструкциям в терминале.

4. **Для продакшена:**
   ```bash
   vercel --prod
   ```

### Способ 3: Drag & Drop

1. Зайдите на [vercel.com](https://vercel.com)
2. Перетащите папку `remont-profi` в окно браузера
3. Дождитесь деплоя

## Настройка домена (опционально)

1. В панели Vercel откройте проект
2. Перейдите в Settings → Domains
3. Добавьте свой домен
4. Настройте DNS записи у регистратора домена

## Кастомизация

### Изменение цветов

В файле `style.css` в начале найдите блок `:root` с CSS-переменными:

```css
:root {
    --primary: #1E88E5;        /* Основной цвет */
    --primary-dark: #1565C0;   /* Темнее */
    --primary-light: #42A5F5;  /* Светлее */
    --secondary: #FF6D00;      /* Акцентный цвет */
    /* ... */
}
```

### Изменение контактов

В файле `index.html` найдите и замените:
- Телефон: `+7 (900) 123-45-67`
- Email: `info@remont-profi.ru`
- Адрес: `г. Москва, ул. Строителей, 15`

### Добавление услуг

В `index.html` в секции `.services-grid` добавьте новую карточку:

```html
<div class="service-card">
    <div class="service-icon">🔨</div>
    <h3 class="service-title">Название услуги</h3>
    <p class="service-description">Описание услуги.</p>
    <a href="#contact" class="service-link">Подробнее →</a>
</div>
```

## Браузерная поддержка

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## Лицензия

MIT License — используйте свободно для любых целей.
